import 'package:flutter/material.dart';

class recipeMinue extends StatelessWidget {
  const recipeMinue({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}